{
  "name": "sync-valuations",
  "version": "1.0.0",
  "description": "Sync Valuations — static site",
  "dependencies": {
    "serve": "^14.2.0"
  },
  "scripts": {
    "start": "serve . -p ${PORT:-3000} --config serve.json"
  }
}
